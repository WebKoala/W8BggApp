<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BGGApp.Controls">

    <Style TargetType="local:StatsBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:StatsBar">
                    <Grid Background="{TemplateBinding Background}" Height="14" Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=MyWidth}">
                       <!-- <TextBlock Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=PercentageDisplay}" HorizontalAlignment="Center"></TextBlock> -->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
